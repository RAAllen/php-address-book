<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="/css/styles.css" rel="stylesheet" type="text/css">
    <title>PHP Address Book</title>
  </head>

  <body>
    <div class="container">
      <h1>Address Book</h1>
      <div class="row">
        <div class="col-xs-6">
          <p>Please enter your contact information below and then hit the add contact button.</p>
          <form action="/added" method="post">
            <div class="form-group">
              <label for="enter_name">Enter your contact's name:</label>
              <input name="enter_name" type="text" placeholder="Betsy Bootstrap eg.">
            </div>
            <div class="form-group">
              <label for="enter_number">Enter your contact's phone number:</label>
              <input name="enter_number" type="number" placeholder="1234567890 eg.">
            </div>
            <div class="form-group">
              <label for="enter_street">Enter your contact's street address:</label>
              <input street name="enter_street" type="text" placeholder="123 Composer Court eg.">
            </div>
            <div class="form-group">
              <label for="enter_city">Enter your contact's city:</label>
              <input name="enter_city" type="text" placeholder="PHPville eg.">
            </div>
            <div class="form-group">
              <label for="enter_state">Enter your contact's state:</label>
              <input name="enter_state" type="text" placeholder="PA eg.">
            </div>
            <div class="form-group">
              <label for="enter_zip">Enter your contact's zip code:</label>
              <input name="enter_zip" type="number" placeholder="12345 eg.">
            </div>
            <button type="submit" name="form-submit" class="btn-success btn-lg">
          </form>

          <p>This button will clear out your address book:</p>
          <form action="/deleted" method="post">
            <button type="submit" name="form-delete" class="btn-danger btn-lg">Delete Contacts</button>
          </form>
        </div>

        <div class="col-xs-6">
          {% if contacts is not empty %}
            <h3>Your existing address book:</h3>
            <ul>
              {% for contact in contacts %}
                <li>{{ contact.getName }}</li>
                <li>{{ contact.getNumber }}</li>
                <li>{{ contact.getStreet }}</li>
                <li>{{ contact.getCity }}</li>
                <li>{{ contact.getState }}</li>
                <li>{{ contact.getZip }}</li>
              {% endfor %}
            </ul>
          {% endif %}

        </div>
      </div>
    </div>
  </body>

</html>
